# AtlasMarketDomain

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build AtlasMarketDomain` to build the library.

## Running unit tests

Run `nx test AtlasMarketDomain` to execute the unit tests via [Jest](https://jestjs.io).
